# T-039 实现验证（2026-08-12）

## 结论

通过。历史补登记现已同时受固定 Git 祖先截止与用户确认约束；门禁启用提交及其后提交不能获得事后豁免，D-20260811-012 的五个既有历史提交仍可重放。

## 验证范围

- `base_commit`: `833f1136b9aeb3a93105fdd5f0d4b6df15a11a5c`
- `head_commit`: `206ac30abef5d756360625f046e4cf12bccc0046`
- `lease_generation`: 1
- 决策：D-20260811-012、D-20260812-019

## 证据

1. `python3 scripts/workflow.py validate --base 833f1136b9aeb3a93105fdd5f0d4b6df15a11a5c --head 206ac30abef5d756360625f046e4cf12bccc0046`：通过。
2. `python3 -m unittest discover -s tests -p 'test_*.py'`：94 项通过。
3. 对抗性覆盖：合法旧提交、非用户确认声明、截止提交本身、截止后或不可达提交。
4. 判定使用 `git merge-base --is-ancestor` 和完整40位截止 SHA，不依赖机器时钟。
5. 决策 schema/统一决策校验与运行时收集器形成双层拒绝；条件不满足时继续普通逐提交校验。

## 加载测试

不涉及 HOI4 内容或运行时文件，无需游戏加载测试。
