# T-015 最终验证

日期：2026-08-11

验证范围：`68464d416af05a01d896a656e19103c1aa4971d8..e2b65309722a20b4e2f17c2df5693de2bbefb9cc`

## 结论

通过。T-015 已按 D-20260811-008 实现，未发现阻止合并的问题。

## 核对结果

| 检查项 | 结果 | 证据 |
|---|---|---|
| Git 输出编码 | 通过 | `run_git` 显式使用 UTF-8，并以 `replace` 容错；单元测试核对 subprocess 参数。 |
| 生命周期提交边界 | 通过 | 四项状态命令统一要求干净 `main`、新鲜的当前机器快照，并只提交任务台账及声明产物。临时 Git 仓库集成测试验证实际提交文件集合与提交后干净状态。 |
| 跨机 handoff | 通过 | 本地或已 fetch 的 `origin` tip 均可验证；两者分歧及 ref 缺失均拒绝；没有隐式网络调用。 |
| 任务隔离与决策 | 通过 | generation、base/head、任务分支 tip 和 D-20260811-008 均在完整提交范围内验证。 |
| 时钟前置条件 | 通过 | 保留 15 分钟与禁止未来时间门禁，README 和框架文档增加 UTC/NTP 同步要求。 |
| OpenCode 运行时权限 | 机器 C 通过 | `execute`/`verify` 的 `env-check` 被允许，Git 命令被拒绝；`verify` 的带 SHA validate 被允许，范围外写入被拒绝。 |
| 工作区边界 | 通过 | 范围内未修改 `Settings/`、`设定书/`、游戏本体、Workshop 或用户目录。 |

## 验证命令

- `python3 -m unittest discover -s tests -v`：47/47 通过。
- `python3 scripts/workflow.py validate`：通过。
- `python3 scripts/workflow.py validate --base 68464d416af05a01d896a656e19103c1aa4971d8 --head e2b65309722a20b4e2f17c2df5693de2bbefb9cc`：通过。
- `git diff --check`：通过。

## 异机后续

机器 A 更新到本次版本后，仍须在 A 上重新运行 `env-check --publish`，并按 `协作/README.md` 对其 OpenCode 版本、权限匹配和中文 Git 路径做实机复测。这是机器 A 的环境确认，不是本次实现缺陷；机器 C 无法替代该证据。
