# T-016 AI 工作流硬约束实现验证

日期：2026-08-11

## 本机实现结论

机器 C 可实施部分通过：待定项结构化门禁、Git index 校验、跨平台 hooks、文档与对抗测试均已完成。未修改 `Settings/`、`设定书/`、游戏本体或任何外部游戏目录。

## 核对项目

| 项目 | 结果 | 证据 |
|---|---|---|
| 待定项净消失检测 | 通过 | 逐 Git diff hunk 抵消仍保留的待定语义，跨 hunk 抵消原行完全相同的搬迁；剩余项生成路径与原行 SHA-256 证据。 |
| 决策语义绑定 | 通过 | `decision.schema.json` 新增 `resolved_pending`；路径必须被同一决策的 `affected_files` 覆盖，次数不足或指纹不符均失败。 |
| 逐提交校验 | 通过 | `validate_commit_rules` 对 `base..head` 中每个设定层 commit 独立检查，后续 commit 不能补授权。 |
| staged 校验 | 通过 | `validate --staged` 读取 Git index 中的文件、决策 JSON、tasks 策略与00卷新增行，不以工作区 diff 代替提交边界。 |
| pre-commit | 通过 | 实际运行 `.githooks/pre-commit`，staged 校验和 54 项标准库测试通过。 |
| hooks 跨平台入口 | 通过 | `run-python` 依次选择 Windows `py -3`、`python3`、`python`；三个 hook 文件均以 executable mode 入库。 |
| 机器 C hooks 启用 | 通过 | 本 clone 的 `core.hooksPath` 已设置为 `.githooks`。 |

## 自动测试

- `python3 scripts/workflow.py validate`：通过。
- `python3 scripts/workflow.py validate --staged`：通过。
- `python3 -m unittest discover -s tests -v`：54/54 通过。
- `.githooks/pre-commit`：真实运行通过。
- 对抗覆盖：无结构化授权、错误 `affected_files`、授权次数不足、仍保留待定语义、跨文件原样搬迁、真实 Git index 中文路径。

## 远端保护状态

仓库文档已加入 main ruleset／分支保护的强制清单。本执行环境没有 GitHub 管理 API 凭据，未能读取或修改仓库 ruleset；此前普通 `git push origin main` 可直接成功，因此在管理员完成远端配置并做失败 PR 冒烟前，不得宣称 CI 不可绕过。

OpenCode plugin 按 D-20260811-009 明确不在本阶段实现。
