# T-031 实现验证（Codex，2026-08-12）

## 结论

PASS（仅限 T-031 静态层）：50 槽引擎硬上限、工厂引擎键名、单州 50 上限、全球 1904 初始工厂总量守恒、现有超限州重分配和新瀛既定疆域落实均通过静态验证。

本结论不宣称方案 A 已全部完成。现有受控快照尚无 `state_category`，因此“每州初始工厂不超过原版州类别基础槽位”仍须由后续快照升级与机器 A 刷新/干跑验证。

## 验证范围

- 租约提交：`927ef6bd9adb08968fc36a37f597d3750c5675be`
- 实现提交：`61c6d14a389bbe762b377e5b88e9dbb29f4b86e0`
- 决策：`D-20260812-014`

## 证据

1. `python3 scripts/workflow.py validate --base 927ef6bd9adb08968fc36a37f597d3750c5675be --head 61c6d14a389bbe762b377e5b88e9dbb29f4b86e0`：PASS。
2. `python3 -m unittest discover -s tests -v`：81/81 PASS。
3. `mod/common/defines/zz_txg_defines.lua` 唯一声明 `NDefines.NBuildings.MAX_SHARED_SLOTS = 50`。
4. 经济清单共 70 个工业州、1904 座共享工厂，最大单州 50；键名仅为 `industrial_complex`、`arms_factory`、`dockyard`。
5. 原 15 个超过 50 的州已消除；California 的 40 座溢出工厂分配到 Oregon 与 Washington，两州同时依既有新瀛疆域设定落实为 XIN。
6. 新增对抗性测试覆盖错误工厂键、51 槽超限、总量漂移及 define 缺失/重复/数值错误。

## 后续门禁

- 扩展受控 state 快照 schema，加入 `state_category` 元数据；机器 A 必须刷新快照。
- 根据原版州类别基础槽位重新分配 1904 座初始工厂，并在转换干跑中检查逐州容量。
- T-028 最终集成仍须经过机器 A 加载测试；机器 C 不具备 `snapshot_export` 与 `load_test`，不得替代。
