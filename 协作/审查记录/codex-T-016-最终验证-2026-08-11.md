# T-016 最终验证

日期：2026-08-11

验证范围：`57b69f908d856d78757824c24742930b53d1c5fc..d0ecbaad0993b36a93ba9ea2233f110e66ecff46`

## 结论

仓库内实现通过，可以合并。远端 GitHub ruleset／分支保护属于仓库外管理状态，本环境没有管理 API 凭据，不能代替管理员配置；在远端失败 PR 冒烟完成前，不得宣称 CI 不可绕过。

## 验证结果

| 检查项 | 结果 | 证据 |
|---|---|---|
| 同 commit 决策关联 | 通过 | D-20260811-009 与 workflow/schema/hooks 核心改动位于同一实现 commit，`affected_files` 覆盖实际路径。 |
| 待定项净消失 | 通过 | 无授权、错误路径覆盖、次数不足均失败；保留待定语义和跨文件原样搬迁不误报。 |
| staged 边界 | 通过 | `validate --staged` 在真实 Git index 上通过；中文设定书路径有临时仓库集成测试。 |
| pre-commit | 通过 | 实际 commit 触发 hook，并完成 staged 校验与 54/54 测试。 |
| pre-push | 通过 | 以 Git 标准 stdin 模拟新任务分支推送，自动求 `origin/main` merge-base 并完成范围校验。 |
| 可执行位与跨平台入口 | 通过 | 三个 hook 均为 mode 100755；入口覆盖 `py -3`、`python3`、`python`。 |
| 机器 C 启用状态 | 通过 | `git config --get core.hooksPath` 输出 `.githooks`。 |
| OpenCode plugin | 按决策不实施 | D-009 明确本阶段优先现有权限、固定导出器和操作系统只读边界。 |

## 自动检查

- `python3 scripts/workflow.py validate`：通过。
- `python3 scripts/workflow.py validate --staged`：通过。
- `python3 scripts/workflow.py validate --base 57b69f908d856d78757824c24742930b53d1c5fc --head d0ecbaad0993b36a93ba9ea2233f110e66ecff46`：通过。
- `python3 -m unittest discover -s tests -v`：54/54 通过。
- `git diff --check`：通过。

## 外部操作项

管理员仍需按 `协作/README.md` 启用 main ruleset：强制 PR、要求 `workflow-integrity / validate`、禁止强推并限制 bypass，随后用一个必然失败的 PR 验证无法合并。机器 A 更新后还需执行 `git config core.hooksPath .githooks`。
