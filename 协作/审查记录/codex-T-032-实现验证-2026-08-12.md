# T-032 实现验证（Codex，2026-08-12）

## 结论

PASS：受控快照 v3 的州类别与基础槽位元数据导出、双指纹过期判断和任务所需快照版本门禁均已实现；现有 v2 快照的迁移兼容保持有效。

## 验证范围

- 租约提交：`b26e6da32db0beb4892cc73026ddb7ac29aee113`
- 实现提交：`df87ecd54f2fb2780e955f08b49fa79109f50e69`
- 决策：`D-20260812-014`

## 证据

1. 完整提交区间统一校验：PASS。
2. Python 单元测试：85/85 PASS。
3. 临时本体 fixture 的 `snapshot-export` 成功生成 schema v3，含 1 个 state 与 1 个 state_category；机器与 JSON schema 双重校验均通过。
4. v3 每州记录 `state_category`；类别表记录 `local_building_slots`、来源相对路径与 SHA-256，不记录颜色或原脚本文本。
5. 未知类别、重复类别、缺少/非整数基础槽位、state/state_category 任一指纹变化均默认拒绝。
6. `inputs.snapshot_schema_version=3` 在当前 v2 快照上会阻止任务输入解析，不能误领取后续工业重分配任务。

## 能力边界

机器 C 无 `snapshot_export`，因此没有改写共享真实快照。机器 A 合并本任务后必须运行 `snapshot-export` 生成 v3，再提交并审查 1081 州的真实类别数据；在此之前严格原版初始槽位重分配保持未完成。
