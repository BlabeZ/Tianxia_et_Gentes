# T-047 工作流漏洞修复验证

> 验证者：Codex（机器 C）  
> 日期：2026-08-13  
> 决策：D-20260813-001  
> 验证区间：`a9389f57b7fa6cad8c69e672740b1e6d19617382..37199fafe63f9954ba9219b09a9d81166b242feb`

## 结论

**静态验证通过。** T-047 已修复审查中确认的 H1、H2、M1、M2、M3，并在 Phase 3 启用前封闭 M4 的路径与可执行文件边界。H3 不改写 T-042 历史，而由规划和本报告继续明确标记 Gate 0、Phase 3/4 尚待机器 A。

本结论不等于 HOI4 实机加载 PASS。机器 C 的公开能力快照为 `load_test=false`，因此未启动游戏；真实三类 profile 三轮稳定性和 T-028 端到端仍不得登记完成。

## 已验证修复

1. `game-test` 改用实时外部能力探测；无 `load_test` 时默认拒绝，具备完整真实路径的隔离夹具可达到 load-test 分支。
2. `task test-result` 只接受 `加载测试-*.json` 与同名确定性 Markdown 报告对；校验 schema、verdict、registrable、task/generation、当前被测提交、mod 树、profile/rules、runner commit、测试启动时环境快照和消费状态。
3. 报告登记只能在原 runner 机器执行，并实时复核 `load_test=true`、受信 `hoi4(.exe)` 及 mod 描述符哈希；普通 Markdown、跨机登记、旧提交、重放和 INCONCLUSIVE 均不能推进状态。
4. 成功登记原子写入 `consumed_by`；若状态提交失败，JSON、Markdown、tasks.json 与任务台账全部恢复，且撤销暂存。
5. 活动任务必须显式声明 `acceptance.requires_load_test`；为 true 时必须要求 `load_test` 能力。
6. 活动任务的 registry outputs 必须与任务书 outputs 完全一致，当前活动任务已同步。
7. 主调度生命周期与 game-test 使用 O_EXCL 原子锁；已有锁默认拒绝且不覆盖，活进程锁必须显式 `--force` 才可清理，锁令牌防止误删其他会话锁。
8. 报告路径经 resolve 后限制在 `协作/审查记录/`，拒绝穿越、覆盖已有报告和任意可执行文件。

## 测试证据

- `python3 scripts/workflow.py validate --base a9389f57b7fa6cad8c69e672740b1e6d19617382 --head 37199fafe63f9954ba9219b09a9d81166b242feb`：PASS。
- `python3 -m unittest discover -s tests -q`：147 项通过。
- `python3 -m compileall -q scripts tests`：PASS。
- `git diff --check`：PASS。
- pre-commit：统一校验与 147 项测试通过后生成实现提交 `37199fa`。
- 独立 CLI 复测：普通验证 Markdown 被报告命名/报告对门禁拒绝；已有 coordinator lock 被默认拒绝且保留；机器 C 的 game-test 被 `load_test=false` 默认拒绝，退出后无残留锁。

## 保留项

- T-042 的历史 `done` 不改写，但不能被解释为 Phase 3/4 已完成。
- Gate 0 的 readiness 基线与用户参数尚未完成；已由 T-048（`decision_required`）明确承接实机进程启动、PID/Job Object 约束、受控退出、日志采集及三轮稳定性，必须由机器 A 实现和验收。
- 在上述保留项完成前，任何真实加载测试 PASS 都必须继续冻结；本次通过只覆盖 T-047 工作流门禁修复本身。
