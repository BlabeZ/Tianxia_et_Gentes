# T-035 独立国家 tag 快照框架实现验证

- 验证者：Codex（机器 C）
- 任务：T-035 generation 1
- 提交区间：`3f1023e14bd6b0e37ef52f9648cf8834e20150dd..891cc8a90aaee8df24d7ae900a8c630112f33d5c`
- 结论：PASS

## 验证证据

1. `python3 scripts/workflow.py validate --base 3f1023e14bd6b0e37ef52f9648cf8834e20150dd --head 891cc8a90aaee8df24d7ae900a8c630112f33d5c`：通过。
2. `python3 -m unittest discover -s tests -v`：88/88 通过。
3. `python3 -m py_compile scripts/workflow.py`：通过。
4. fixture 导出覆盖完整 definition/history、缺 definition、缺 history；输出不含本机绝对路径或脚本正文。
5. 对抗性测试覆盖重复 tag、`../` 越界、绝对路径、存在状态与 SHA 矛盾。
6. 实现仅新增独立 `country-tags.json` 契约；未修改 `states.json` schema 或五项环境能力布尔值。

## 能力边界

机器 C 的 `snapshot_export=false`，本次只验证导出器与 fixture。真实 HOI4 tag/definition/history 清单必须由机器 A 后续只读导出并单独审查。
