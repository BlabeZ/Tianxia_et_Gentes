# T-049 意识形态闭环修复验证

- 验证日期：2026-08-14
- 验证机器：C / Codex
- 环境能力：`partial`；`mod_execution=true`、`static_validation=true`、`load_test=false`
- 决策依据：`D-20260813-002`
- 提交区间：`e507e768c452f50a8f83b70b38736a746ff8acba..c2a1c67ca02f962e8281a36b73c54f5cc34aebcf`
- 结论：静态验证通过；T-049 可合并。真实 HOI4 解析与运行未在机器 C 上执行，不以静态结果替代机器 A 门禁。

## 已修复问题

1. 明确区分 HOI4 原生四极与项目党派 ID：`set_politics.ruling_party` 只允许 `communism/democratic/fascism/neutrality`，项目党派、subtype、五轴和八类旗标由 `TXG_set_project_party` 独立维护。
2. `on_startup` 与 `on_ruling_party_change` 改为 fail-closed 校验；状态缺失或四极失配时清除派生分类并留下显式错误旗标，不从同极反推具体项目党派。
3. 好感入口移除固定 `close` 占位，改为参数化档位；真实国家对生成继续由 T-046 g2 在 T-045 明确开局党派后完成。
4. 补齐中英文 40 个 subtype 名称/描述及相关提示文本，并把本地化完整性纳入静态门禁。
5. 政治光谱 schema 现要求决策号、标题、非空数据，并用 `oneOf` 区分默认坐标文档和国家党派文档；自带校验器补齐本地 `$ref`、`oneOf`、上下限及对象字段数支持。
6. 新增党派坐标推导四极与 subtype 所属四极一致性检查，以及国家历史文件非法 `ruling_party`/缺少项目状态同步入口检查。
7. 任务书 schema 强制 `limits/revert_on_fail/checkpoint_policy`；T-045 明确 17 个国家 tag，并把文件上限从不可能完成的 8 调整为 20，同时新增 `max_files >= scope.tags` 门禁。
8. 修复任务重新领取后残留验证报告、generation 递增时旧提交污染输出区间的问题。
9. 修复历史交接单与重开任务当前状态错误比对的问题：历史交接仍验证结构、隔离命名与真实提交差异，但不再冒充当前 generation。
10. T-046 原 `pending_test` 交付确认只是未调用框架，已失效为 `stale/g2`；T-045/T-046 均显式依赖 T-049，T-046 g2 还依赖 T-045。

## 验证证据

- `python3 scripts/workflow.py validate --base e507e768c452f50a8f83b70b38736a746ff8acba --head c2a1c67ca02f962e8281a36b73c54f5cc34aebcf`：通过。
- `python3 -m unittest discover -s tests -v`：156 项全部通过。
- `git diff --check e507e768c452f50a8f83b70b38736a746ff8acba..c2a1c67ca02f962e8281a36b73c54f5cc34aebcf`：通过。
- 回归负例覆盖：空政治光谱文档、跨极党派坐标、项目党派误写原生四极、缺失原子同步入口、任务书缺少失败控制、generation 输出污染、历史交接误绑定当前任务。

## 原分析结论的校正

T-045 的交接单由生命周期命令在任务分支之外生成，不计入任务分支 `max_files`；因此旧报告中“17 个国家文件加交接单至少 18 个”的表述应校正为“任务分支至少需要 17 个国家文件”。但旧上限 8 仍客观不可完成，现已改为 20，并由 tag 数量门禁防止再次出现同类矛盾。

## 尚未解除的机器 A 门禁

1. T-043：以当前原版 `00_ideologies.txt` 做精确差异和通用 subtype 兼容检查，并执行真实加载。
2. T-045：从受控国家快照生成 17 个完整国家历史文件；开局执政党和支持度必须有明确来源，不得猜测，并统一调用 `TXG_set_project_party`。
3. T-046 g2：在 T-045 数据落地后生成真实国家对距离/好感调用块，再执行开局、换党与日志验证。
4. T-048：继续完成 Gate 0 与真实启动参数基线；机器 C 的 156 项静态测试不能代替以上加载测试。

