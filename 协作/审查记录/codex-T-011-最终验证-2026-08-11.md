# T-011 全量 state 归属映射最终验证

日期：2026-08-11

机器：A

能力：`profile=full`；`snapshot_status=current`；`snapshot_export/mod_execution/static_validation/load_test=true`。

## 验证范围

- 任务：`T-011`，generation 1。
- 内容基线：`50a2d0fbb63a74d701b0b0fe42757a96f60cfa9b`。
- 原子租约提交：`c2735138bd77b4d07185328675e544c170c803be`。
- 任务成果：`b16b723994e4a04ec97bc3ce1ebfe3902453aefc`。
- 实际施工文件：`协作/扫描产出.md`。
- 决策依据：`D-20260811-024`；交接范围修复：`D-20260811-027`。

## 数据验收

| 项目 | 结果 |
| --- | --- |
| 受控快照指纹 | `ba683a184d14e58d6df9e1636d372a2771b558edd9a77baa7c13de9521888b8f`，与当前 A 机实测一致 |
| state 覆盖 | 1081/1081，每个 `state_id` 恰好一次 |
| 快照字段 | `state_id`、`localisation_key`、快照名称、相对文件逐行反校验通过 |
| owner_tag | 仅使用 T-010 的 38 tag 白名单或文档级 `BACKGROUND` |
| 明确归属 | 594 行，均提供设定书卷条引用 |
| 待精调 | 487 行 `BACKGROUND`，均登记 Nudge 说明 |
| 零行 tag | `JAP/IND/KOK/BUR`；表示当前 state 粒度无法唯一落界或政权仍为背景层，不表示取消政权 |
| 本体访问 | 未读取或复制游戏本体 state 正文；只消费已提交的受控快照元数据 |
| 任务范围 | 原子租约提交之后到 head 只有 `协作/扫描产出.md`，符合 `max_files=1` |

## 自动验证

- 自定义逐行反校验：1081 行、1081 个唯一 ID、名称/路径/白名单/引用/备注全部通过。
- `python scripts/workflow.py validate --base 50a2d0f... --head b16b723...`：通过。
- `python -m unittest discover -s tests -v`：72/72 通过（含 handoff v1/v2 兼容修复测试）。
- `python scripts/workflow.py validate`：通过。
- `git diff --check`：通过。

## 结论

PASS。T-011 可进入 `ready_to_merge`。本任务验收明确不运行 `state-build`，加载测试留待 T-028 生成完整 states 后统一执行。
