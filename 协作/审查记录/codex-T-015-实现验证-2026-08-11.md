# T-015 跨机工作流加固实现验证

日期：2026-08-11

机器：C

能力：`dialog_development=true`、`static_validation=true`；本机不具备本体快照、mod 执行或加载测试能力。

## 修复核对

| 项目 | 结论 | 证据 |
|---|---|---|
| Windows Git 输出编码 | 已修复 | `run_git` 固定 `encoding="utf-8"`、`errors="replace"`，并有 subprocess 参数回归测试。 |
| 生命周期原子提交 | 已修复 | `handoff / validation-result / test-result / complete` 统一检查干净 `main`、当前机器新鲜快照和实际暂存文件集合，再自动提交台账及状态产物。 |
| 跨机任务分支 | 已修复 | handoff 接受本地或已 fetch 的 `origin/task/...` tip；两者存在且不同即拒绝；不隐式联网。 |
| 跨机时钟 | 已补充操作门禁 | README 与框架文档明确要求 NTP 或系统时间服务同步 UTC；代码继续拒绝未来及超过 15 分钟的快照。 |
| OpenCode 权限 | 机器 C 实测通过 | 真正经 Task 工具调用 `execute`、`verify` 子代理；不是用 `--agent` 直接启动后回退默认 agent。 |

## 自动验证

- `python3 -m unittest discover -s tests -v`：47/47 通过。
- `python3 scripts/workflow.py validate`：通过。
- `git diff --check`：通过。
- 临时 Git 仓库集成测试确认 validation 状态提交只包含：当前环境快照、`tasks.json`、派生台账及声明的审查报告，提交后工作区干净。

## OpenCode 运行时权限冒烟（机器 C）

| 子代理 | 实际调用 | 结果 |
|---|---|---|
| `execute` | `python3 scripts/workflow.py env-check` | 允许，退出码 0。 |
| `execute` | `git status --short` | 在执行前被 bash 权限策略拒绝。 |
| `verify` | `python3 scripts/workflow.py env-check` | 允许，退出码 0。 |
| `verify` | `git status --short` | 在执行前被 bash 权限策略拒绝。 |
| `verify` | `python3 scripts/workflow.py validate --base <40位SHA> --head <40位SHA>` | 允许，校验通过。 |
| `verify` | 写入 `协作/权限冒烟-应拒绝.tmp` | 在写入前被权限策略拒绝，文件未创建。 |

注意：OpenCode 1.18.9 中 `verify`/`execute` 是 subagent；`opencode run --agent verify` 会回退默认 agent，不能作为权限验证。必须由主 agent 通过 Task 工具实际委派。

## 尚需异机确认

机器 A 的 OpenCode 版本、权限运行时及 UTF-8 Git 路径输出只能在机器 A 上实测。本次已修复代码与文档，但不能在机器 C 上伪造机器 A 的运行结果。机器 A 更新后应依照 `协作/README.md` 的冒烟清单复测并重新发布环境快照。
