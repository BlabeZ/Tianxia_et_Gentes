# 验证-T036：机器A导出真实国家tag快照

> 验证人：opencode（机器A/主调度器）｜日期：2026-08-12
> 任务：T-036 g1｜base: e6123f5｜head: 60c997e

## 验证清单

1. **设定来源可追溯**：D-20260812-016（国家层独立快照授权）；本任务不触碰设定书。
2. **仓库写入路径符合权限**：交付仅 `协作/扫描快照/country-tags.json` + `country-tags-summary.md`，与任务书 outputs 完全一致（handoff schema v2 强制校验）。
3. **设定层改动同步**：不涉及。
4. **待定项门禁**：不涉及（无 Settings/设定书 变更）。
5. **租约匹配**：generation=1、分支 task/T-036-g1 tip=60c997e、base=e6123f5（assign 时 main）全部一致。

## 内容核对

- 439 个注册 tag；每个 tag 含 tag、registry 相对路径、registry SHA-256、definition 存在状态、history 存在状态
- definition 缺失 0；history 缺失 75（未使用 tag 正常形态）
- 输出仅含脱敏元数据，无国家脚本正文泄漏；独立 schema 与语义校验通过（validate PASSED）
- game_version=Operation Postern v1.19.2.0.a729 (d245)，generated_by_machine=A

## 结论

PASS。不涉及游戏加载（任务书 acceptance.load_test=不涉及）。
