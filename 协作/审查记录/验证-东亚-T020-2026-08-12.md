# 验证报告：T-020 东亚 state 归属声明

- 任务：T-020 g1
- base_commit：1c4f4b486713603ecd3812e2a2020ba6f4ebf835
- head_commit：74346c5cfced676e516022f92beb3adf81a961eb
- decision_id：D-20260811-023

## 验证项

1. **设定来源可追溯**：全部 82 条 override 的 owner/controller/add_core_of 均来自 T-011 映射表（`协作/扫描产出.md`，绑定快照指纹 `ba683a18…`）与 `设定书/08-地理卷.md` 核心判定（内地15行省+边疆政区=CHI；朝鲜/越南=独立政权；新瀛/新燕=海外顺土）。
2. **快照绑定**：source_fingerprint 与 `协作/扫描快照/states.json` 一致；每条 override 的 state_id/source_relative_path/source_sha256 均与快照逐条核对通过。
3. **schema 与语义**：`state-overrides.schema.json` + `validate_override_document` 通过；无重复 state/path。
4. **核心判定**：owner 均同时存在于 add_core_of（无背景层条目——东亚明确归属州均为核心/顺土；JAP/BUR 在扫描产出为 BACKGROUND，本任务不声明）。
5. **范围**：仅新增 `协作/state-overrides/东亚.json`（任务书 outputs 白名单内）。
6. **统一校验**：`validate --base --head` 通过（WORKFLOW VALIDATION PASSED）。

## 验证结论

PASS。声明清单可进入受控生成阶段（T-028 在机器 A 汇总全部地域后统一生成）。
