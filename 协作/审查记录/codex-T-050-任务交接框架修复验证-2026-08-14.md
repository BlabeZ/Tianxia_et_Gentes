# T-050 任务书与交接框架修复验证

- 日期：2026-08-14
- 机器：C / Codex
- 决策：D-20260814-001
- 环境：partial；`static_validation=true`、`load_test=false`
- 结论：审查确认的 P0/P1 协作层漏洞已完成代码修复和自动回归；HOI4 实机测试不属于本任务。

## 修复结果

1. 生命周期事务：assign、heartbeat、reclaim-stale、handoff、validation-result、test-result、complete、checkpoint 的写入均由统一事务保护。失败时恢复起始 HEAD、受控分支引用、index 和允许路径原文；complete 的真实失败 hook 回归证明任务书归档与台账零残留。
2. 续租与回收：两者现在先执行 main/干净工作区/环境门禁，再原子提交任务状态；无过期租约时不制造空提交。任务分支上的两条真实 CLI 冒烟均以退出码 2 拒绝。
3. 合并门禁：新增受控 `task merge`、`.githooks/pre-merge-commit` 与 `merge-check`；候选 SHA 由调度命令显式传入，失败自动 abort。统一校验器同时拒绝 `pending_validation/pending_test` head 已进入 main。T-043 的既成异常逐 head 绑定 D-20260814-001，继续停留 pending_test。
4. 提交图完整性：非 legacy 的 base/head/checkpoint 现在必须引用真实提交并满足祖先链；done head 必须进入 main。T-004 错误 SHA 已更正为 `3219b93ae9462bcedaa57ab5b09867af39203e63`。
5. 静态验证报告：新增 `validation-report.schema.json`、JSON/Markdown 可重复渲染、task/generation/base/head/runner/环境/真实命令绑定和 `consumed_by` 防重放；普通 Markdown 不再能推进状态。
6. 跨机同步：新增 `sync-check`，跨机 assign/handoff 与 test-result 在 origin/main 双向不一致时默认拒绝。本机真实结果为落后 0、领先 12，退出码 2，符合预期。
7. 权限与说明：execute/scan/verify 均显式 `external_directory: deny`；verify 只增加 `验证-*.json` 和受控 renderer 的窄权限。OpenCode debug 解析可见项目 deny，primary `task` 运行确认 env-check/区间 validate 可执行；被禁止探针由子代理主动拒绝，因此未把该轮表述为宿主工具层拒绝证据。
8. 元数据：T-043 补 Gate 0 blocker，T-045 module 与任务书标题同步，T-046 stale 清除旧 g1 validation_report，台账已重新生成。

## 测试矩阵

| 检查 | 结果 |
| --- | --- |
| `python3 scripts/workflow.py validate` | PASS |
| `python3 scripts/workflow.py validate --ci` | PASS |
| `python3 -m unittest discover -s tests -v` | 169/169 PASS |
| `python3 -m compileall -q scripts tests` | PASS |
| `git diff --check` | PASS |
| complete 提交 hook 失败回滚 | PASS；HEAD、任务书、index、工作树恢复且 Git 状态干净 |
| 通用事务提交后异常回滚 | PASS；新提交和临时任务引用均撤销 |
| 非 main heartbeat/reclaim CLI | PASS；均拒绝且无工作区变化 |
| 提前合并/不存在 SHA/报告重放/错误命令绑定 | PASS；均被回归测试拒绝 |
| `sync-check` 本地领先场景 | PASS；准确拒绝并报告 ahead=12 |

## 保留事项

- T-043 仍需 T-048 Gate 0 和机器 A 真实加载测试；本任务未伪造 PASS。
- GitHub main ruleset 状态仍需仓库管理员在远端确认；本地 hook 可被 `--no-verify` 绕过，远端保护仍是最终强闸门。
- 当前本地 main 尚未推送，`sync-check` 会有意阻断跨机状态推进，直至显式同步远端。
