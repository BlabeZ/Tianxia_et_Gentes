# D-20260812-021：需求登记层建立与任务书目录迁移授权

> 状态：已确认｜确认者：用户｜时间：2026-08-12T08:40:33Z

## 通俗解释

给仓库加一层"需求清单"（大项目定义），任务书按需求分文件夹存放，做完的自动收进归档抽屉。本次一次性完成：登记 5 个需求（初始地图/工业槽位/国家快照/领导人/基础设施）、任务书搬入子目录并标注归属、schema 升级强制"任务必须属于某需求"、任务完成后自动归档。改动全部放进一个提交，避免半途状态导致校验失效。

## 正式表述

1. 需求登记层 `需求/`：R-001~R-005，requirement.schema.json 校验。
2. `需求/` 入 CORE_PATTERNS（变更须同 commit 决策）。
3. 任务书按需求子文件夹分批 + 22 份单 commit 迁移 + done 入 `_归档/`。
4. task-spec v3 必填 requirement_ref；tasks 增 requirement_id；孤儿拒绝。
5. complete 自动归档（新旧路径对）；归档层仅格式校验；审计证据永久保留。
6. T-041 州界重划仅登记，实现留阶段 1。
7. 原子 commit 落地。

## 实现修订

任务书归档由 `git mv` 改为纯文件 `replace`（避免 index 先行变更导致提交范围暂存失败）。

## 影响文件

`需求/`、`schemas/`（requirement/task-spec/tasks）、`scripts/workflow.py`、`任务书/`、`协作/tasks.json`、`协作/README.md`、`tests/test_workflow.py`
