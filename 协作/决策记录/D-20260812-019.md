# D-20260812-019：历史补登记截止提交与用户确认双门禁

> 状态：已确认｜确认者：用户｜时间：2026-08-12T07:34:39Z

## 通俗解释

历史补登记只能给旧门禁启用前的提交补票，而且必须由用户亲自确认。门禁启用以后产生的违规提交不能再靠事后决策记录逃过检查。

## 正式表述

1. 以 `33ed2312245caa7c5cd089cd63b8a085570cb74c` 为逐提交决策门禁启用点，仅其严格祖先可进入 `historical_backfill`。
2. 含历史豁免声明的决策必须满足 `status=confirmed` 且 `confirmed_by=user`。
3. 使用 Git 祖先关系而非提交时间判定截止边界；条件不满足时不授予豁免，继续执行普通逐提交校验。
4. T-039 在 schema/统一决策校验与运行时收集器两层拒绝非用户声明，并以94项单元测试覆盖合法旧提交、非用户声明、截止点本身及截止后提交。

## 关联范围

- T-039 历史补登记门禁加固
