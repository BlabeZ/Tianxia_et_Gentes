# D-20260815-002：T-048 增量日志算法修复（system.log 清空重写视为预期轮转）

> 状态：已确认｜确认者：用户（长程自动化指令）｜时间：2026-08-15T16:50:00Z

## 通俗解释

加载测试第一次真跑时判"证据不足"，原因是游戏的 system.log 每次启动都会清空重写（不是追加），程序的"只读新增部分"算法把它当成了没有新日志。修复：遇到文件变小就认为是"清空重写"从头读，并作为预期的轮转行为记录，不再误判为证据中断。

## 正式表述

1. 实机首轮（T-028 map-load）INCONCLUSIVE：`logs无新增证据`；根因 HOI4 启动重建 system.log（基线 §2 每轮清空），`appended = max(0, new - base) = 0`。
2. 修复：轮询中 `size < offset` → 重置偏移从头读；diff 中 head 哈希变化且为清空重写 → `expected_rotation=True`（continuity 保持）；清空后未重写也计轮转证据。
3. `LogDiff` 增加 `expected_rotation`；`all_consistent` 判定排除预期轮转。
4. 169 项单测全绿；同一实现批次延续 D-20260815-001。

## 影响文件

- `scripts/workflow.py`：轮询偏移重置 + diff 轮转标记 + all_consistent
- `scripts/game_test.py`：LogDiff.expected_rotation
