# D-20260815-005：mod txt 文件 UTF-8 BOM 清理（引擎 parser 兼容）

> 状态：已确认｜确认者：用户（长程自动化指令）｜时间：2026-08-15T17:40:00Z

## 通俗解释

第三轮测试：脚本效果文件开头藏了一个不可见字符（BOM），游戏解析器不认它，把整个文件读错并崩溃。已批量清理模组里所有文本文件的这类字符。

## 正式表述

1. 归因：crash dump hoi4_20260816_092619；error.log `00_txg_regime_effects.txt near line: 8 ( 锘? )`（UTF-8 BOM）→ 27 errors → PHYSFS 崩溃。
2. 修复：剥离 mod/**/*.txt 的 `\xef\xbb\xbf` 前缀（仅 regime 文件受影响）。
3. 同批次：模板占位、BOM、schema 扩展均为 T-043/T-048 加载链路修复。

## 影响文件

- `mod/common/scripted_effects/00_txg_regime_effects.txt`
