# D-20260811-024：T-011 受控 state 归属映射施工格式

> 状态：已确认｜确认者：主 agent（依据用户“按顺序进行完成”指令）｜时间：2026-08-11T15:15:10Z

## 通俗解释

先做一张覆盖全部 1081 个原版州的总清单。每个州只能出现一次，能按现有设定明确归给 38 个正式政权的就写对应 tag；属于背景势力或现在还不能确定边界的先写 `BACKGROUND`，并注明以后需要在 Nudge 阶段精调。这个步骤只整理地图施工底稿，不直接改游戏州文件。

## 正式表述

1. T-011 在正式领取前建立 schema v2 任务书，并绑定当前受控快照。
2. 映射表穷尽快照全部 1081 个 state，保持 state_id 唯一。
3. `owner_tag` 白名单为 T-010 锁定的 38 个 tag；`BACKGROUND` 是文档级哨兵值，不是游戏 tag。
4. 归属依据限于 08 地理卷和 11 政权清单；不能唯一确定的州不得猜测，只登记 Nudge 精调点。
5. 本任务不读取本体 state 正文，不写 override，不生成 `mod/history/states/`。

## 影响文件

- `任务书/T-011.json`
- `协作/tasks.json`
- `协作/任务台账.md`
