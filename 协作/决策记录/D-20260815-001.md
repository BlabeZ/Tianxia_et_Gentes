# D-20260815-001：T-048 Phase 3/4 实机执行实现与基线 markers 落地

> 状态：已确认｜确认者：用户（长程自动化指令）｜时间：2026-08-15T16:30:00Z

## 通俗解释

机器 A 在 8-14/15 通过 20 多次真实运行摸清了游戏加载模组的机制（dlc_load.json 无 BOM + pdx_ 命名注册文件）。本次把游戏自动化测试执行器从"只查配置"升级为"真刀真枪跑游戏"：自动激活模组、启动游戏、盯日志等就绪标志、超时受控关闭、把每次会话写成可审计的报告。同时把 Gate 0 基线确定的日志标志（模组已加载/主菜单就绪/地图加载完成）和致命/忽略规则填进受控配置。

## 正式表述

1. `run_game_test_session`：完整状态机（PREFLIGHT→BASELINE→LAUNCHING→WAITING_READY→SOAKING→STOPPING→COLLECTING→WRITING_REPORT）；`subprocess.Popen([exe, "-debug"])`、增量日志轮询（2s）、`taskkill /PID /T /F` 仅本 PID 树、用户目录前后快照（门禁 A）、报告 JSON+MD 原子写并经 `redact_dict` 脱敏。
2. mod 激活预置：`write_dlc_load`（无 BOM dlc_load.json）+ `write_mod_registry`（pdx_ 注册文件），机制依据基线报告 §6b，属 D-20260814-002 受控例外。
3. profiles：menu-debug/map-load 的 required_markers 依基线 §2（Active Mod Count/CFrontEnd/Loaded provinces）；fatal（crash/state）+ ignore（ugc 注册/physfs/occupation 提示）依基线 §3；process-smoke 空 markers 豁免（registrable=false）。
4. local.schema.json：`mod_registry_name` 约束 `^(pdx|ugc)_[0-9]+\.mod$`。
5. 依据用户"长程自动化"指令登记；不影响此前任何决策。

## 影响文件

- `scripts/workflow.py`：run_game_test_session + game_test_command 实机路径 + preflight 空 markers 豁免
- `scripts/game_test.py`：write_dlc_load/write_mod_registry/snapshot_user_docs/diff_user_docs
- `config/game-test-profiles.json`：基线 markers/rules 填充
- `schemas/local.schema.json`：mod_registry_name
