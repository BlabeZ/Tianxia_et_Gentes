# D-20260814-001：全面修复任务书与交接框架冒烟缺陷

> 状态：已确认｜确认者：user｜时间：2026-08-14T02:17:27Z

## 通俗解释

用户确认按冒烟审查结果全面维修协作框架。本次会让任务状态更新真正做到“要么全部成功、要么恢复原样”，不再允许续租和回收在错误分支留下散落改动，也会阻止未验收的任务内容偷偷进入主线。新的静态验证报告必须像加载测试报告一样对得上任务、代数和提交，而且只能使用一次。

T-043 已经提前进入主线的事实不会通过改写 Git 历史隐藏；系统会把它登记为一次明确的历史异常，并继续要求真正的加载测试。跨机器工作前也会明确检查本地与远端主线是否双向一致。

## 正式表述

实施统一生命周期事务回滚、main 分支持久化门禁、合并前与全库提前合并检测、非 legacy 提交图引用校验、结构化静态验证报告对及一次性消费、跨机 origin/main 同步检查和 OpenCode 外部目录默认拒绝。同步修复 T-004、T-043、T-045、T-046 已确认的元数据异常。

既有 T-043 提前合并使用本决策作显式迁移豁免；豁免只承认已经发生的单个 head，不允许未来任务复用。历史验证 Markdown 保持可读，但新的 `validation-result` 一律采用结构化 JSON/Markdown 报告对。

## 影响范围

- R-005 / T-050 协作层基础设施修复
- 任务生命周期、交接、验证、测试与合并门禁
- OpenCode subagent 权限和跨机器同步入口
