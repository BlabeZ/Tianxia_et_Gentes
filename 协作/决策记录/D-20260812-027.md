# D-20260812-027：T-032解析器真实本体兼容修复（state_categories包装块解包）

> 状态：已确认｜确认者：用户｜时间：2026-08-12T15:05:00Z

## 通俗解释

T-032 做的快照导出程序，测试时用的是它自己编的示例格式；但游戏本体里 13 个州类别文件的真实写法外面都多套了一层 `state_categories={...}` 的壳。机器 A 第一次真跑就发现解析器不会剥壳，直接报错。本次修复让解析器认得这层壳，同时保留对旧示例格式的兼容，并补了一个回归测试。

## 正式表述

1. 机器 A 实测：HOI4 v1.19.2 本体 `common/state_category/` 13/13 文件均为 `state_categories={...}` 包装格式；T-032 fixture 为无包装格式，两者结构不同导致真实导出在 `city.txt` 首次运行即失败（"state_category 必须有唯一标量 local_building_slots"）。
2. 修复 `parse_state_category_file`：顶层存在唯一 `state_categories` 包装块且内部非空时解包一层，再按类别块解析；无包装旧格式继续支持，既有测试不受影响。
3. 新增 `test_state_category_parser_accepts_state_categories_wrapper` 回归测试（105 项全绿）。
4. 该修复为 T-033 执行前置条件，独立提交至 main；T-033 租约恢复 todo 后重新分配执行。

## 影响文件

- `scripts/workflow.py`：parse_state_category_file 包装块解包
- `tests/test_workflow.py`：新增包装格式回归测试
- `协作/tasks.json`：T-033 恢复 todo（重新分配）
- `任务书/R-002-工业槽位/T-033.json`：inputs 复位
