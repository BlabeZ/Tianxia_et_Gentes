# D-20260812-054：T-044 放宽对 T-043 的任务依赖：数据契约先行

> 状态：已确认｜确认者：用户｜时间：2026-08-12T20:40:00Z

## 通俗解释

坐标数据契约（T-044）是纯数据层工作，不需要游戏实机验证。它原先依赖 T-043 只是因为要用 40 个子类型的 key 清单，而这清单已经写好落盘了。拍板：把这条依赖放开，本机先做 T-044；万一 T-043 在机器 A 实测后改了什么 key，T-044 跟着微调就行。

## 正式表述

1. T-044 为纯数据层任务（acceptance.load_test 不适用）；对 T-043 的依赖仅因 40 子类型 key 清单，该清单已落盘（`docs/意识形态-跨极映射表.md`，T-043 static 验证 PASS）。
2. 放宽 T-044 对 T-043 的任务依赖，本机先行；T-043 实机 load_test 若修改子类型 key，T-044 契约同步微调。
3. T-046 对 T-043 的依赖保持不变。

## 影响文件

- `协作/tasks.json`
