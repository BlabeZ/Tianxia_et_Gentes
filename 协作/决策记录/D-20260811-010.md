# D-20260811-010：历史遗留登记补登与机器B登记/冒烟验证修复

> 状态：已确认；确认者：用户；时间：2026-08-11T08:10:00Z

## 正式表述

本次提交完成三项工作：

1. **历史遗留登记补登**：为 5 个历史 commit 补登同 commit 决策授权（校验器逐 commit 要求决策 JSON）：0320d50 机器A一致性审查登记、5635265 协作框架强化（修订记录同步铁律+硬约束1挂钩 skills+验证清单第3项双证）、9d36aef 卷二建议稿升格、5bc7c07 09卷待定清单结案、e178b8f skill 跨机共享。补登行登记于 `设定书/00-总览与索引.md` 修订记录表并标注"补登"。
2. **机器B登记**：新建 `.opencode/local.json`（machine_id=B，windows），启用入库 Git hooks（`.githooks`），发布脱敏能力快照 `协作/环境/B.json`（profile=light：dialog_development/static_validation 可用，snapshot_export/mod_execution/load_test 因无游戏本体路径如实为 false）。
3. **冒烟验证修复**：机器B冒烟测试发现并修复 Windows 专属 bug——`scripts/workflow.py` 中 `task_assign`/`commit_lease_and_create_branch` 用 `str(relative_to)` 得到反斜杠路径，与 git 输出的正斜杠路径比较失败，改为 `.as_posix()`；`tests/test_workflow.py` 真实 git 仓库 helper 缺少 `encoding="utf-8"`，Windows 下解码中文路径失败，与同文件另一 helper 对齐。修复后单元测试 54/54 通过。冒烟验证另发现：MCP filesystem 写工具（`filesystem_write_file` 等）权限键为工具名本身，落入 subagent 配置首条 `"*": allow`，可绕过 `edit: deny` 边界——**用户决定暂不修复，仅记录**（见 `协作/审查记录/opencode-机器B登记与冒烟验证-2026-08-11.md`）。

## 通俗解释

把机器 B 登记进协作环境（能力如实上报，不虚报）；冒烟测试时顺手修了两个只在 Windows 上才出现的 bug（路径斜杠、中文编码），所有测试已通过；5 个以前漏登记的历史改动这次补上了登记；发现的权限漏洞按你的要求先记下来不修。
