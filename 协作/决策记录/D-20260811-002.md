# D-20260811-002：任务租约自动提交与本地分支创建

> 状态：已确认｜确认者：用户｜时间：2026-08-11T02:20:38Z

## 正式表述

`task assign` 在干净的 `main` 分支上执行能力与依赖校验，写入租约状态后自动创建本地租约提交和对应任务分支。远程推送仍为主调度器的显式操作。交接时必须验证 head 是记录分支的实际 tip。

## 通俗解释

发任务时，脚本会一次完成“记账、提交账本、建好任务分支”；它不会悄悄推到远程。交作业时，记录的分支和实际提交必须对得上。
