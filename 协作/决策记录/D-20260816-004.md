# D-20260816-004 — T-046 g2 好感网络生成定案

- **状态**：confirmed（主 agent 无人值守授权拍板）
- **日期**：2026-08-16
- **范围**：T-046 / 好感网络 / on_actions

## 背景

T-046 g1 已交付框架（距离表 1560 对、6 档 opinion modifiers、政体判定、fail-closed 校验），g2 需完成好感网络落地（依赖 T-045 执政党数据）并接线钩子。任务书 decision_points「好感权重系数须拍板」已由 D-20260812-063 用户拍板。

## 决定

1. 权重沿用 D-20260812-063：same_subtype=+40 / close=+20 / neutral=0 / distant=-10 / opposite=-30 / foreign_gap=-10；同子类型优先。
2. **有向 240 对**：16 国（MEX 待定排除）每国对全部其他 15 国 remove 6 档 + add 命中档；KR 模式换党重算。
3. 落点 `mod/common/scripted_effects/00_txg_opinion_network.txt` + on_actions 双钩子分派。
4. validate 闭环：`validate_opinion_network` 从国家历史→坐标表→重算→比对生成文件 + on_actions 分派检查。

## 影响

- 开局各国好感按坐标距离生成（同坐标近=友好）；换党即时重算。
- 生成文件与坐标表/国家历史三方一致性由 validate 强制。
