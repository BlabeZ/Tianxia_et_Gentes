# D-20260811-022：机器A登记为full机并导出首次受控快照，修复导出器schema_version

> 状态：已确认｜确认者：用户｜时间：2026-08-11T13:45:00Z

## 通俗解释

机器 A 是本项目唯一装有《钢铁雄心4》游戏本体的电脑，之前一直没登记。这次：

1. **登记机器 A**：填好本机路径配置，实测出"能读游戏本体、能生成模组、能跑加载测试、能静态校验"全套能力，成为 full 档机器（另外两台 B/C 只是 light 档，只能做文字和校验工作）。
2. **装了新 Python**：本机原来的 Python 3.9 太旧，项目要求 3.10+，已升级到 3.12，以后命令行统一用它。
3. **第一次真正导出游戏快照**：把游戏里全部 1081 个地区的元数据（编号、省份列表、校验值）导入仓库，供其他机器和后续任务使用。
4. **修了一个隐藏 bug**：导出程序写的版本号还是旧的 v1，与校验规则要求的 v2 不符；以前从没真机跑过所以没被发现，这次第一次真跑就暴露并修复了。

## 正式表述

1. 机器 A 登记：`.opencode/local.json`（machine_id=A，game_path/workshop_path/user_docs_path 实测可读），`env-check --publish` 发布 `协作/环境/A.json`：profile=full，五项能力全 true，snapshot_status=current。
2. 运行环境：Python 3.12.10（py launcher 默认 -V:3.12，3.9.13 保留未卸载）。
3. `snapshot-export` 首跑：1081 states / 10272 provinces，source.fingerprint=ba683a184d14e58d6df9e1636d372a2771b558edd9a77baa7c13de9521888b8f，game_version=Operation Postern v1.19.2.0.a729 (d245)。
4. 缺陷修复：`snapshot_export()` 生成 `schema_version: 1` → `2`，与 snapshot.schema.json v2（D-20260811-018）对齐；B/C 机无 game_path，此前导出路径从未被执行，本次为首次实测。
5. 提交身份：仓库级 `user.name=BlabeZ`、`user.email=zhengweifeng051922@163.com`（用户拍板，区别于历史作者 weifeng.zheng）。

## 影响文件

- `scripts/workflow.py`：schema_version 1→2（1 行）
- `协作/环境/A.json`：新增（full 档快照）
- `协作/扫描快照/states.json` + `states-summary.md`：新增（受控快照 v2）
- 本决策记录 D-20260811-022
