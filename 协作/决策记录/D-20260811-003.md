# D-20260811-003：验证通过后使用待合并状态

> 状态：已确认｜确认者：用户｜时间：2026-08-11T02:26:03Z

## 正式表述

任务在静态验证和必要的加载测试通过后进入 `ready_to_merge`，不提前标记 `done`。主调度器显式合并任务分支；`task complete` 验证交接 head 已进入 `main` 后，才将任务置为 `done`。验证或测试失败时，任务在原隔离令牌内回到进行中并重开租约。

## 通俗解释

作业验收合格后先放到“待合并”架子上，不会提前打勾完成。只有代码真的进了主线，账本才会显示完成。
