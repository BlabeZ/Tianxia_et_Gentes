# D-20260811-015：机器B复核落地（gitattributes/Python版本要求/UTF-8输出）

> 状态：已确认；确认者：用户；时间：2026-08-11T08:40:00Z

## 正式表述

依据机器 C 的两份复核（`codex-机器B登记与冒烟复核-2026-08-11.md`、`opencode-2026-08-11-Windows平台兼容审查.md`），在机器 B 落实未闭合项：

1. **P2（hooks LF 保护）**：新增 `.gitattributes`——`* text=auto` 统一文本换行规范，`.githooks/* -text` 强制 hooks 保持 LF，Windows `core.autocrlf` 不再可能改写 shell hook。
2. **P3（Python 版本文档化）**：AGENTS.md「工作约定」与 `协作/README.md`「开工入口」记录 **Python 3.10+** 要求（脚本使用 PEP 604 语法），Windows 统一 `py -3`。
3. **低优先（冒烟范围）**：`协作/README.md` 权限冒烟验证范围由"机器 A/C"更新为"机器 A/B/C"（机器 B 已登记）；hooks 段补充 autocrlf 建议（`false` 或 `input`）。
4. **P5（Windows 输出编码）**：`scripts/workflow.py` 的 `main()` 入口对 `stdout/stderr` 执行 `reconfigure(encoding="utf-8")`（Python 3.7+，失败静默忽略），消除 Windows 管道/重定向时的 GBK 乱码。
5. **快照刷新**：`py -3 scripts/workflow.py env-check --publish` 刷新 `协作/环境/B.json`（checked_at 更新，满足 15 分钟新鲜度要求）。

P1（`as_posix` 路径修复）已在 af35e34 落地并经复核确认有效；P4（CI 增加 windows-latest job）已由机器 C 在 9408a59 落实，本机复核确认存在。

## 通俗解释

把机器 C 复核时提出的 Windows 隐患逐项处理：hooks 文件不会再被 Windows 自动换行弄坏、文档写明要用 Python 3.10+、权限冒烟说明补上机器 B、脚本输出中文不再乱码，机器 B 的快照也刷新了。
