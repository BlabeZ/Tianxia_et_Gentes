# D-20260812-029：T-032解析器真实本体兼容修复（三）——重复 state_category 后者生效

> 状态：已确认｜确认者：用户｜时间：2026-08-12T15:20:00Z

## 通俗解释

机器 A 连续排查后发现第三种情况：极少数州文件（1081 个里 4 个）把州类别字段写了两遍。其中 3 个两遍一样（如都是 town），1 个（433 号拉杰普塔纳州）两遍不一样（一遍 rural 一遍 town）。经过拍板：两遍一样的正常取该值；两遍不一样的按游戏引擎的规则取后写的那一遍（引擎本来就是后写覆盖先写），并提示警告。

## 正式表述

1. 真实本体 4/1081 个 state 文件重复声明 `state_category`：190-Kurzeme（rural/rural）、440-West Punjab（town/town）、816-West Virginia（town/town）重复值一致；433-Rajputana（rural/town）矛盾。
2. 用户拍板（推荐项）：一致重复取唯一值；矛盾重复按 HOI4 解析语义后者生效。
3. `parse_state`：声明数 0 → 报错；≥1 → 取 `category_matches[-1]`（后者生效），重复时输出 WARNING（含文件与全部声明值）。
4. 新增 `test_parse_state_duplicate_category_takes_last`（覆盖一致与矛盾两分支）；全套 107 项测试通过。
5. 延续 D-20260812-027/028 处置：main 独立提交，T-033 恢复 todo 后重新分配。

## 影响文件

- `scripts/workflow.py`：parse_state 重复声明处理
- `tests/test_workflow.py`：新增重复声明回归测试
