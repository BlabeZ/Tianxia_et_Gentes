# 机器 A 开工简报（2026-08-13，主 agent 整理）

> 供机器 A 的 opencode 按序执行；开工先 `py -3 scripts/workflow.py env-check --publish` 刷新能力快照。

## 任务队列（依赖序）

| 序 | 任务 | 当前状态 | A 机动作 | 关键命令/注意点 |
| --- | --- | --- | --- | --- |
| 1 | T-043 | pending_test | 实机加载验证意识形态无错误 → `task test-result --result pass` | 四极壳 rules 按原版兼容意图重建，需与本体 `00_ideologies.txt` diff 核对；报告落 `协作/审查记录/加载测试-*.md` |
| 2 | T-028 | todo（条件全就绪） | assign → 干跑 → 正式生成 → load_test | deps 10 项全 done；inputs fingerprint 与 A 机 current 快照一致；先 `state-build --dry-run --override <8份清单>` 看差异统计（新增 1081/修改/遗留），确认后去掉 --dry-run；T-034 的日本三州条目已在经济与工业.json 内（282=5民4军/1018=2民/1019=3民1军），随整包一起生成 |
| 3 | T-045 | todo（deps 已满足） | assign → 读本体 `history/countries/*.txt` 完整改写 | 政党 key=坐标表 ID（`协作/政治光谱/坐标-国家政党.json` 56 条）；HOI4 历史文件同名整体覆盖（缺字段即默认），必须基于原版完整改写；set_politics 初始化国家变量 `TXG_axis_e/p/f/l/o`（供 T-046 政体判定读取）+ 打 8 大旗标（`TXG_clear_government_flags` 配套） |
| 4 | T-046 | pending_test | T-045 完成后：生成器填充好感网国家对档位 PDX 块 → 实机开局好感验证 → test-result | 生成器函数 `workflow.opinion_band_for`；分档 modifier 已定义（`mod/common/opinion_modifiers/00_txg_opinions.txt`） |
| 5 | T-041 | todo（依赖 T-028） | T-028 后 assign | 州界走向设定级决策须退回主 agent 拍板；provinces 校验升级 |
| 6 | T-042 Gate 0 | todo | 基线侦察：真实启动 argv、mod-loaded 标志、readiness 标志、用户目录运行时写入边界 → 回报主 agent | 侦察结果用于 2 项前置拍板（用户目录写入授权、ASCII 测试路径方案） |

## 顺序依赖提醒

- **T-045 必须先于 T-046 的 load_test**（好感网验证依赖政党数据）
- T-028 完成后 `replace_path="history/states"` 缺口才补齐，mod 才能加载开局
- T-034 已 done（日本 15 厂声明+容量门禁校验器），无需 A 机操作，随 T-028 生成即可

## MVP 铁律（D-20260812-064）

批次全部 done 后必须执行最小可运行验收：① 加载无 state/ideology/country/scripted 解析错误；② replace_path 缺口补齐；③ 1910 开局进入主地图；④ 坐标驱动机制运行无报错。未通过按 fail_semantics 退回。

## 本机（C）已完成、无需 A 机重复

- T-008/T-038/T-044 全 done；T-034 done（本轮）
- 五轴坐标/距离表/政党契约、8 大聚合 trigger、政体判定旗标框架均已落盘并经 116 项单测
- 人物表 46+7 人（D-061/067）；巴西工会领袖、新瀛海商领袖两项留待主题阶段
