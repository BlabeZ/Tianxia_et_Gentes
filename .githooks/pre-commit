#!/bin/sh
set -eu

repo_root=$(git rev-parse --show-toplevel)
cd "$repo_root"
runner="$repo_root/.githooks/run-python"

"$runner" scripts/workflow.py validate --staged
"$runner" -m unittest discover -s tests -v
